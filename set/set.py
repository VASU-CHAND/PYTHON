# a set is the unordered elements its
#  mutable 
# dont allow duplicate values
#stores unique elements
#element dont have index position
# only immutable elements are allowed
s={}
print(type(s))
s=set()
print(type(s))
